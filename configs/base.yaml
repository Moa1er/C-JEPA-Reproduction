seed: 42
device: "cuda"
num_workers: 4
pin_memory: true

data:
  index_file: "data/train.jsonl"
  video_root: "C:/Users/SIC 187/Downloads/video_train/video_09000-10000"
  video_glob: "video_09060.mp4"
  split_name: "train"
  clip_len: 16
  resize: 224
  interpolation: "bilinear"
  mean: [0.485, 0.456, 0.406]
  std: [0.229, 0.224, 0.225]

encoder:
  checkpoint_path: ""
  input_key: "frames"
  output_slots_key: "slots"
  slot_masks_key: "slot_masks"
  expected_num_slots: null
  expected_slot_dim: null
  # TODO: Replace this placeholder loader with validated
  # SlotFormer/SAVi checkpoint loading snippet and exact return-key mapping

run:
  batch_size: 2
  max_videos: 1
  output_dir: "outputs/features/train"
